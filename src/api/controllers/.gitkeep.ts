/**
 * Controllers — Request handlers that parse input, call services, and return responses.
 *
 * Example:
 *   export async function createPayment(req: Request, res: Response, next: NextFunction) { ... }
 */
